[2026-06-02 20:28] patch: Register nice-react-form in the package registry
[2026-06-12 19:21] patch: --build-all prints a per-package built/skipped/failed list with skip/fail reasons after the summary
[2026-06-19 18:52] minor: --reset now terminates any running `ntk --dev`/`--watch` first (SIGTERM, then SIGKILL on stragglers) so their rollup watchers do not race the rebuild or have node_modules mutated under them
[2026-06-24 20:40] patch: Register nice-react-tooltip in the package registry
[2026-07-01 17:27] patch: Remove nice-react-device-detector from the package registry — the package is retired and its detection logic was internalized into nice-react-styles' StylesProvider
[2026-07-01 17:27] patch: Publish now restores file: deps in a finally AND on SIGINT/SIGTERM, so an interrupted (Ctrl-C at the OTP prompt), failed, or errored publish no longer strands semver dep ranges on disk (which previously got committed and broke local npm install). Restore stays before the version-bump commit so it still records file: refs.
[2026-07-01 19:20] minor: Add --build-icons — rebuilds nice-icons and its dependents (nice-react-icon, nice-react-icon-vendor, nice-react-button) in tier order via the same reverse-dependency graph --publish uses, for a targeted build after an SVG/icon change instead of a full --build-all
[2026-07-03 12:21] minor: --build-icons now auto-stops a concurrent dev watcher. When a running ntk --dev/--watch is detected (both rebuild the same dist and race), it terminates it first — the same reflex as the --vite port-kill — then builds and reminds you to restart it. Pass --no-kill to build anyway.
[2026-07-03 14:32] minor: Add --log flag to --reset — captures each build's output and writes a timestamped, shareable report (build/skip/fail counts + full failure output per package) to {workspace}/.nice/reset-{timestamp}.log.
[2026-07-03 14:32] patch: Add @types/react and @types/react-dom to the dedupe singleton list. A duplicate physical @types/react copy (even same version) gives TypeScript two FC identities, so @rollup/plugin-typescript fails a component's declaration emit (TS2883) and the dts bundle breaks. Now deduped like react itself.
[2026-07-03 15:30] minor: ntk --build-icons --convert [path] passes --convert through to nice-icons only, converting new/changed .source .ai files before the icon rebuild. The optional path scopes it — a folder or a single .ai file; omit for all. buildPackages gained a per-package extraArgs option to route it.
