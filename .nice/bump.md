[2026-05-24 06:45] patch: Defer npm auth check in `--publish` from step 0 to step 8 (right before the actual `npm publish` invocation). Scan/display/prompt/build phases no longer block on `npm login` since they don't need npm credentials. If the user isn't logged in, they'll hit the failure at OTP time instead of upfront — saves rerunning the whole prompt+build flow after logging in.
[2026-05-24 06:30] minor: Add `--reset` flag — chains `--build-all → --dedupe → --clean` in sequence. The recommended workflow after refactors that touch foundation packages (token renames, type narrowing in nice-styles). Order is fixed: rebuilds first, then dedupe to catch any singletons introduced during the rebuild cascade, then clean to flush dev-server caches. Exit code reflects `--build-all`'s result; dedupe and clean always run.
